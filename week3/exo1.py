# Write a function named times_ten. The function should accept an argument and display the product of its argument multiplied times 10.
def ten_times(tt):
    print(tt*10)

b = float(input('Give a number'))
ten_times(b)


#Examine the following function header, and then write a statement that calls the function, passing 12 as an argument.
def show_value(quantity):
    print(quantity)

show_value(12)


#Look at the following function header:
#def my_function(a, b, c):
#Now look at the following call to my_function:
#my_function(3, 2, 1)
#When this call executes, what value will be assigned to a? What value will be assigned to b? What value will be assigned to c?

def my_function(a, b, c): 
    print(3, 2, 1)

my_function(4, 3,1)




